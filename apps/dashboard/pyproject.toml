[project]
name = "label-hold-dashboard"
version = "0.2.0"
description = "Label Hold Dashboard BFF — FastAPI serving UI + JSON proxy + Firestore read."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.32",
  "google-cloud-firestore>=2.20.0",
  "httpx>=0.27",
  "python-multipart>=0.0.9",
]
